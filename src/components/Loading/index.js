import styles from './Loading.module.css';

function Loading() {
  return (
    <main className={styles.LoadBackground}>
      <div className={styles.Loading}></div>;
    </main>
  );
}
export default Loading;
